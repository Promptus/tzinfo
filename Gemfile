source "https://rubygems.org"

# Note that I had to install ruby manually via rvm and then remove the installed rake and manually install this old rake version.
# Also had to comment out tasks in the Rakefile
# But then you can run "rake build_tz_modules" to convert IANA files into working ruby files
ruby "1.8.7"
gem "rake", "0.8.7"
